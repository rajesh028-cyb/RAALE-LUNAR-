# Docker Infrastructure Configurations

This folder contains service specific configurations, certificates, or initialization files for our Docker containers.
- The Dockerfiles for backend services are maintained inside their respective codebase directories (`backend/api/Dockerfile`, `backend/loader/Dockerfile`).
- The Dockerfile for the frontend is maintained in `frontend/Dockerfile`.
